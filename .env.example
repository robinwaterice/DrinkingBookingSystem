# 辦公室飲料訂購系統 - 環境變數設定範例

# 說明：
# 本系統使用 Google Apps Script (GAS) 搭配 Google 試算表作為資料庫後端。
# 如果您要部署自訂的後端，請複製此檔案並命名為 `.env.local` 或直接修改 `index.html` 中的 `API_URL` 變數。

# Google Apps Script 部署為 Web 應用程式的 URL 網址
VITE_GAS_API_URL="https://script.google.com/macros/s/您的部署ID/exec"
